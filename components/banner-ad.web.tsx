/**
 * Web用バナー広告コンポーネント（空）
 *
 * react-native-google-mobile-ads はネイティブ専用のため、
 * Web環境では何も表示しません。
 * Metroバンドラーは .web.tsx を優先的に読み込むため、
 * ネイティブモジュールのインポートエラーを回避できます。
 */
export default function BannerAdComponent() {
  return null;
}
